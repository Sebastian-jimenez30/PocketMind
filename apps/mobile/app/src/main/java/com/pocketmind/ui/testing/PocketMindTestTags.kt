package com.pocketmind.ui.testing

/**
 * Stable semantics identifiers for instrumentation tests. They are kept
 * separate from user-facing copy so tests survive wording and localization
 * changes.
 */
object PocketMindTestTags {
    const val HOME_CONTENT = "home_content"
    const val PROFILE_CONTENT = "profile_content"
}
